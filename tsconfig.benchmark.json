{
  "extends": "./tsconfig.base.json",
  "include": ["benchmark/**/*"],
  "exclude": ["node_modules"],
  "compilerOptions": {
    "lib": ["esnext", "dom"],
    /* Module Options */
    "module": "CommonJS" // Node.js の ES Modules 対応は安定していないので CommonJS を使う
  }
}
